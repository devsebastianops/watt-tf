transform:
  - target: resource.aws_s3_bucket.main
    value:
      bucket: "${input.project_name}-bucket"
  - target: resource.aws_s3_bucket.main
    value:
      tags:
        Environment: "${input.environment}"
        Project: "${input.project_name}"
