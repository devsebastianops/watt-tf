transform:
# The backticks ensure "company.com" remains a single JSON key
- target: "resource.aws_instance.web.tags.`company.com/managed-by`"
  value: "WattTF"
  
# Useful for complex cloud annotations
- target: "kubernetes_deployment.api.metadata.annotations.`cert-manager.io/cluster-issuer`"
  value: "letsencrypt-prod"